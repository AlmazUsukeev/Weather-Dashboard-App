<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetrofitTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.RetrofitTask" parent="Base.Theme.RetrofitTask" />
    <style name="add_button" parent="ThemeOverlay.Material3.FloatingActionButton.Primary">
        <item name="background">@color/white</item>
        <item name="android:shape">oval</item>
        <item name="icon">@android:drawable/ic_input_add</item>
    </style>
</resources>